<%= form_for(@slh_host) do |f| %>
  <% if @slh_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slh_host.errors.count, "error") %> prohibited this slh_host from being saved:</h2>

      <ul>
      <% @slh_host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
