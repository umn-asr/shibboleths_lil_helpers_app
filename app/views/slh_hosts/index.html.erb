<h1>Listing slh_hosts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @slh_hosts.each do |slh_host| %>
  <tr>
    <td><%= link_to 'Show',user_slh_config_dir_slh_strategy_slh_host_path(@user,@slh_config_dir,@slh_strategy, slh_host) %></td>
    <td><%# link_to 'Edit', edit_slh_host_path(slh_host) %></td>
    <td><%# link_to 'Destroy', slh_host, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%# link_to 'New Slh host', new_slh_host_path %>
