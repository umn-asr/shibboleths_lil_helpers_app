<%= form_for(@slh_site) do |f| %>
  <% if @slh_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slh_site.errors.count, "error") %> prohibited this slh_site from being saved:</h2>

      <ul>
      <% @slh_site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
