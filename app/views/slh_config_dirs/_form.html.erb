<%= form_for [@user,@slh_config_dir] do |f| %>
  <% if @slh_config_dir.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slh_config_dir.errors.count, "error") %> prohibited this slh_config_dir from being saved:</h2>

      <ul>
      <% @slh_config_dir.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    The config name:<%= f.text_field :name %>
    <br />
    The config.rb: <br />
    <%= f.text_area :config_dot_rb,  :size => '80x45' %>
    <br />
    <%= f.submit "save" %>
  </div>
<% end %>
